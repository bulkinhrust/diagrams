import React from 'react';

import classes from './Diagrams.module.scss';

type Props = {};

const Diagrams: React.FC<Props> = (props) => {
  const {} = props;

  return (
    <div className={classes.component}>
      Diagrams
    </div>
  );
};

export default Diagrams;
